services:
  caddy:
    image: caddy:2.8
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
<<<<<<< HEAD
    environment:
      - EMAIL=${EMAIL}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./site:/srv:ro
      - caddy_data:/data
      - caddy_config:/config
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:80 || exit 1"]
      interval: 60s
      timeout: 5s
      retries: 3
    networks:
      - pdfislemleri_network

  api:
    container_name: pdfislemlericom-api
    build:
      context: ./app
      dockerfile: Dockerfile.dev  # Development Dockerfile kullan
=======
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ./site:/usr/share/caddy:ro
    networks:
      - pdfislemleri_network

  pdfislemleri-api:
    build: ./app
>>>>>>> ac16e15 (Your descriptive commit message)
    restart: unless-stopped
    expose:
      - "2000"
    networks:
      - pdfislemleri_network

networks:
  pdfislemleri_network:
    driver: bridge
